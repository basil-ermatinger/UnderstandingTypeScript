"use strict";
// Index types
let store = {};
store.id = 5;
store.isOpen = false;
if (store.isOpen) {
    console.log(`Store with Id ${store.id} is open`);
}
else {
    console.log(`Store with Id ${store.id} is closed`);
}
